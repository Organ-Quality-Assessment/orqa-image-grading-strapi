'use strict';

/**
 * gradingimages service
 */

module.exports = {
    gradingImages: async () => {
        const totalImagesRequired = 5;
    // get which organs to include and decide how many per organ

    // get all images for each organ type

    // select the n least graded images for each organ type

    // get scoring required for each organ type and package with the image

    // return list of filenames + scoring for each image
    }
};
